run:
  timeout: 3m
  tests: false

linters:
  enable:
    - gofmt
    - goimports
  disable:
    - typecheck
    - govet
    - staticcheck
    - unused
    - errcheck

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
